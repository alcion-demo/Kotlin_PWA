spring:
  datasource:
    url: jdbc:postgresql://db:5432/todo
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    cache: false

server:
  address: 0.0.0.0
  port: 8080

